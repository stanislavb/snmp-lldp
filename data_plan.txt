Single host:	info
		interfaces ->
		neighbours ->
		vlans ->

Interface:	number
		name
		speed
		neighbour?
		MAC

Neighbour:	local interface?
		interface
		interface desc
		sysdesc
		hostname

Int-VLAN map:	tuple of (interface, vlantag, encapsulated) = (1, 1, false)
